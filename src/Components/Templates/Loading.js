import loading from '../../Assets/loading.svg';
const Loading = () => {
    return (
        <div className='text-center mx-auto'>
            <img src={loading} />
        </div>
    );
}

export default Loading;